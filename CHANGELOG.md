# Changelog

## [0.3.3] - 2026-08-27

### <!-- 0 -->⛰️  Features

- A peer follows a machine that moved
- A drop says what machine it stands on
- A machine vouches for the drops on it
- A machine names itself when nothing is written down
- A machine is named by the machine
- A save is heard, not waited for
- A namespace can say it needs settling
- A note that is saved often does not grow for ever
- A folder several people share
- An archetype written in lua
- A file several people write at once
- A namespace several machines hold
- What happened to a thing, in order
- A namespace made from the command line
- Connect to an address, and one noun per group
- The keys are behind ?, and there is no box
- Whether to say which networks you are on
- A namespace is drawn by its archetype
- A directory is walked from the command line
- A files namespace is walked, not pushed at
- A terminal takes its shape, and your keys
- Reach a device that cannot be dialled
- Users first, machines inside them
- Trust, and a screen to manage somebody
- Ask for a locked path, answer what is asked
- A path can be seen without being open
- Sign badges with a command, not an agent
- One key per person, and profiles to test with
- What knocked and was turned away
- Your own files namespace lists what is in it
- A device that is off still opens
- Pair with a machine, not its owner
- What is kept on disk can be sealed
- A data key, wrapped and unwrapped
- Who may reach a path, and changing it
- The list groups by person
- Let somebody in, and shut them out
- Rules name people, not only machines
- A person, and a badge for each machine
- Your machines above everybody else's
- Walk a device's paths like folders
- Start from the address that answered last time
- Read back through a conversation
- A line to write on, always there
- A bar down the side of every message
- A screen for each kind of path

### <!-- 1 -->🐛 Bug Fixes

- Two senders do not share one part file
- One guess is paid for once
- One data key, however many drops start
- A machine gets one identity, however many drops start
- Calling something a fold is not a way past the limits
- One watcher cannot finish a namespace for everybody
- A session ends with everything it started
- Bad bytes on the disk are not worth a panic
- Bytes must be what the change asked for
- A stranger cannot name how much memory to hold
- A refused machine stays refused
- A fold cannot wedge a namespace for good
- A listed name is one name in a directory
- A cleaned path is one Clean would accept
- A holder key is matched, not printed
- The holder list is a peer's words too
- One machine's keys cannot be mistaken for each other
- What a peer says cannot draw on your screen
- Two processes cannot lose each other's work
- A machine that moved cannot eat a pairing
- A peer cannot write on your terminal
- Two drops on one machine are two drops
- One machine, several people, several names
- A change says what it is about
- A failure names what it dialled, and stops
- A refusal says whether it is settled
- An announcement says where its sender is
- One bad message costs one message
- A screen is no larger than a screen
- A revoke below a mount is honoured
- A transfer writes only its own part
- What is pinned is what is published
- A guess costs this machine once, not twice
- A part file is one drop made itself
- An overlay is not a bridge
- A session that ends is not a fault
- A device says which networks it is on
- Everything started is waited on and stopped
- Nothing that arrives lands on what is there
- Who you paired with is who the wire says
- A remote screen cannot slice past its row
- Join a terminal by its screen, not its tail
- Leaving a live path must not take it down
- Hold connections the way the daemon does
- Say where you are once you know
- Machines hang off a person, not a hole
- Re-read the address book before answering
- Find a device that is not on the same wire
- A path with more under it can still be opened
- A rendezvous that is not there is not called
- A ticket is who, not where

### <!-- 2 -->🚜 Refactor

- A namespace is an instance of an archetype
- A namespace has an archetype
- One join, not two that drift
- Drop compatibility with versions that never shipped

### <!-- 3 -->📚 Documentation

- One document per subject
- What is bounded before anybody is let in
- The build is .make.lua, not the go tool
- Say what the tools are actually for
- Go build, not make
- What names a machine, and how one moves
- Several people changing one thing
- What it takes to keep a directory in step
- The commands that walk a directory
- A namespace is an instance of an archetype
- The plan, from identity to who may reach what
- One plan, from identity to what is kept on disk
- Encrypting what is kept on disk
- The user key is an ssh key

### <!-- 4 -->⚡ Performance

- Borrow the daemon's connections
- Stay connected to the devices you paired with
- Show it at once, send it on a kept connection

### <!-- 5 -->🎨 Styling

- One ground under everything said
- The line to write on is a box like the rest
- The name is a tag in the bar's colour
- A message sits inside its box

### <!-- 6 -->🧪 Testing

- A change a peer sends, fuzzed
- The parsers a stranger reaches, fuzzed
- Count the guesses instead of timing them
- Fuzz what a peer's bytes are parsed by
- Pin that a badge-less frame is refused

### <!-- 7 -->⚙️ Miscellaneous Tasks

- Regenerate the changelog
- Drop the tools nothing runs
- Say what it is without naming a product
- A second machine, on another chip
- Stop packing the binary
- Drop a stray test file
- Keep PLAN.md out of the repo


### <!-- 0 -->⛰️  Features

- A peer follows a machine that moved
- A drop says what machine it stands on
- A machine vouches for the drops on it
- A machine names itself when nothing is written down
- A machine is named by the machine
- A save is heard, not waited for
- A namespace can say it needs settling
- A note that is saved often does not grow for ever
- A folder several people share
- An archetype written in lua
- A file several people write at once
- A namespace several machines hold
- What happened to a thing, in order
- A namespace made from the command line
- Connect to an address, and one noun per group
- The keys are behind ?, and there is no box
- Whether to say which networks you are on
- A namespace is drawn by its archetype
- A directory is walked from the command line
- A files namespace is walked, not pushed at
- A terminal takes its shape, and your keys
- Reach a device that cannot be dialled
- Users first, machines inside them
- Trust, and a screen to manage somebody
- Ask for a locked path, answer what is asked
- A path can be seen without being open
- Sign badges with a command, not an agent
- One key per person, and profiles to test with
- What knocked and was turned away
- Your own files namespace lists what is in it
- A device that is off still opens
- Pair with a machine, not its owner
- What is kept on disk can be sealed
- A data key, wrapped and unwrapped
- Who may reach a path, and changing it
- The list groups by person
- Let somebody in, and shut them out
- Rules name people, not only machines
- A person, and a badge for each machine
- Your machines above everybody else's
- Walk a device's paths like folders
- Start from the address that answered last time
- Read back through a conversation
- A line to write on, always there
- A bar down the side of every message
- A screen for each kind of path

### <!-- 1 -->🐛 Bug Fixes

- Two senders do not share one part file
- One guess is paid for once
- One data key, however many drops start
- A machine gets one identity, however many drops start
- Calling something a fold is not a way past the limits
- One watcher cannot finish a namespace for everybody
- A session ends with everything it started
- Bad bytes on the disk are not worth a panic
- Bytes must be what the change asked for
- A stranger cannot name how much memory to hold
- A refused machine stays refused
- A fold cannot wedge a namespace for good
- A listed name is one name in a directory
- A cleaned path is one Clean would accept
- A holder key is matched, not printed
- The holder list is a peer's words too
- One machine's keys cannot be mistaken for each other
- What a peer says cannot draw on your screen
- Two processes cannot lose each other's work
- A machine that moved cannot eat a pairing
- A peer cannot write on your terminal
- Two drops on one machine are two drops
- One machine, several people, several names
- A change says what it is about
- A failure names what it dialled, and stops
- A refusal says whether it is settled
- An announcement says where its sender is
- One bad message costs one message
- A screen is no larger than a screen
- A revoke below a mount is honoured
- A transfer writes only its own part
- What is pinned is what is published
- A guess costs this machine once, not twice
- A part file is one drop made itself
- An overlay is not a bridge
- A session that ends is not a fault
- A device says which networks it is on
- Everything started is waited on and stopped
- Nothing that arrives lands on what is there
- Who you paired with is who the wire says
- A remote screen cannot slice past its row
- Join a terminal by its screen, not its tail
- Leaving a live path must not take it down
- Hold connections the way the daemon does
- Say where you are once you know
- Machines hang off a person, not a hole
- Re-read the address book before answering
- Find a device that is not on the same wire
- A path with more under it can still be opened
- A rendezvous that is not there is not called
- A ticket is who, not where

### <!-- 2 -->🚜 Refactor

- A namespace is an instance of an archetype
- A namespace has an archetype
- One join, not two that drift
- Drop compatibility with versions that never shipped

### <!-- 3 -->📚 Documentation

- What is bounded before anybody is let in
- The build is .make.lua, not the go tool
- Say what the tools are actually for
- Go build, not make
- What names a machine, and how one moves
- Several people changing one thing
- What it takes to keep a directory in step
- The commands that walk a directory
- A namespace is an instance of an archetype
- The plan, from identity to who may reach what
- One plan, from identity to what is kept on disk
- Encrypting what is kept on disk
- The user key is an ssh key

### <!-- 4 -->⚡ Performance

- Borrow the daemon's connections
- Stay connected to the devices you paired with
- Show it at once, send it on a kept connection

### <!-- 5 -->🎨 Styling

- One ground under everything said
- The line to write on is a box like the rest
- The name is a tag in the bar's colour
- A message sits inside its box

### <!-- 6 -->🧪 Testing

- A change a peer sends, fuzzed
- The parsers a stranger reaches, fuzzed
- Count the guesses instead of timing them
- Fuzz what a peer's bytes are parsed by
- Pin that a badge-less frame is refused

### <!-- 7 -->⚙️ Miscellaneous Tasks

- Drop the tools nothing runs
- Say what it is without naming a product
- A second machine, on another chip
- Stop packing the binary
- Drop a stray test file
- Keep PLAN.md out of the repo

## [0.3.2] - 2026-08-24

### <!-- 0 -->⛰️  Features

- Show what arrives while you are watching
- One frame, keys at the bottom, drop opens it

### <!-- 1 -->🐛 Bug Fixes

- A live screen fits its panel

### <!-- 5 -->🎨 Styling

- The terminal's own sixteen colours

## [0.3.1] - 2026-08-24

### <!-- 1 -->🐛 Bug Fixes

- Share a terminal while the daemon runs

### <!-- 6 -->🧪 Testing

- Drive two nodes through the whole command line

## [0.3.0] - 2026-08-24

### <!-- 0 -->⛰️  Features

- Send files and links, and fix live views

### <!-- 2 -->🚜 Refactor

- Drop the browser and phone interfaces

## [0.2.4] - 2026-08-24

### <!-- 0 -->⛰️  Features

- Cards a step above the page
- Open the camera from Go, no Java
- Read a pairing code with the camera
- One dark theme, and an icon with nothing behind it

## [0.2.3] - 2026-08-24

### <!-- 0 -->⛰️  Features

- A maskable icon for an installed page
- The logo as the launcher icon
- The real mark, and a dial that survives resumption
- Take a code, not only show one
- Pair both ways, drop's own mark, a better page

### <!-- 1 -->🐛 Bug Fixes

- One accept loop, pair from either side

## [0.2.2] - 2026-08-23

### <!-- 0 -->⛰️  Features

- Pair from the page, with a code that scans
- Pair from the interface, and answer while it is open

### <!-- 1 -->🐛 Bug Fixes

- One listener, a shorter ticket, a code that fits
- Let the defaults serve a paired device

## [0.2.1] - 2026-08-23

### <!-- 0 -->⛰️  Features

- A node on android, drawn by the same go
- Claim a share, and draw a terminal
- One go interface, served as webassembly
- Show the same page, not a second ui

### <!-- 1 -->🐛 Bug Fixes

- Let the archive bring its own classes
- Build the android archive, not an apk
- Keep the entry points out of the go build
- Rebuild while drop is running

### <!-- 7 -->⚙️ Miscellaneous Tasks

- Linux and android only

## [0.2.0] - 2026-08-23

### <!-- 0 -->⛰️  Features

- A share client, and releases that carry it
- Take what a phone shares, and be installable
- Walk paths that nest, with rules under test
- Enter a device, three-line rows
- Titled panes, glyphs and a header
- Drop in a full-screen terminal
- Draw the screen back as ansi
- Show only what a device shares with you
- Deny by default, and enforce it
- Access as an inherited rule on the tree
- Hash the secrets that guard a path
- Darker violet, one radius, one pane on narrow
- Show this device, and a violet UI
- Navigate a device by its namespaces
- Let a paired peer ask what is served
- Serve the page to the network on request
- Keep the terminal screen in go
- Browse drop from this machine
- Find a device that moved
- Serve a terminal read as asciicast
- Own local discovery instead of shared mDNS
- Peer-to-peer transfer, streams and chat over iroh

### <!-- 1 -->🐛 Bug Fixes

- Let the page scroll instead of overflowing
- Let hidden panels actually stay hidden
- See sources under src/pkg and the assets
- Skip the relay when the peer is on this wire
- Announce on a tick instead of once
- Run the mDNS listener instead of waiting on it
- One terminal fanned out to every watcher

### <!-- 3 -->📚 Documentation

- Describe drop as it is now
- Note where the terminal screen lives
- Describe iroh and the pair rendezvous

<!-- WARP -->
