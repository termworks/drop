<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only the window behind Compose: everything drawn is Compose's own theme. -->
    <style name="Drop" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
